/*
 * NIDAR-SENSE main.c integration.
 *
 * Do not replace generated main.c. Add only these statements to the existing
 * CubeMX USER CODE regions.
 */

/* USER CODE BEGIN Includes */
#include "tof.h"
#include "serial.h"
/* USER CODE END Includes */

/* USER CODE BEGIN 2 */
MX_USB_DEVICE_Init();

if (TOF_Init() != HAL_OK)
{
    Error_Handler();
}

if (SERIAL_Init() != HAL_OK)
{
    Error_Handler();
}
/* USER CODE END 2 */

/* USER CODE BEGIN 3 */
TOF_Process();
SERIAL_Process();
/* USER CODE END 3 */
